#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
float Calculate_Value(const vector<int>& sequence, const int & current_pos,const vector<vector<float>>& weight){
	for(int i=0; i != current_pos; i ++){

	}
} 
void Back_Track(vector<int>& sequence, const int & n, vector<int>& bestseq, int & besttime, const int& current, const vector<int, vector<float>> weight){
	if(current == n){
		if()
	}
}
int main(){

}
